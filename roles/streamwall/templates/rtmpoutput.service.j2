[Unit]
Description=RTMP output to {{ item.name }}
PartOf=stream.target
After=streamdelay.service

[Service]
Restart=always
RestartSec=1s
ExecStart=/home/streamwall/rtmpoutput.sh {{ item.url }}
Environment=GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0/

[Install]
WantedBy=stream.target
